import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.0xn33t.ioswidgetsexample',
  appName: 'IOSWidgetsExample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
